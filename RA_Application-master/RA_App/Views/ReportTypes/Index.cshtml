@model IEnumerable<RA_App.Models.ReportType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Category_Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Category_Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Report_ID }) @*|
            @Html.ActionLink("Details", "Details", new { id=item.Report_ID }) |*@
            @*@Html.ActionLink("Delete", "Delete", new { id=item.Report_ID })*@
        </td>
    </tr>
}

</table>
